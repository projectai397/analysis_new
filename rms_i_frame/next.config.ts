import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
  /* config options here */
  // eslint: {
  //   // ⚠️ This will skip ESLint checks entirely
  //   ignoreDuringBuilds: true,
  // },
};

export default nextConfig;
